# Brand and showcase assets

- Runtime brand assets live in [`public/branding`](../../public/branding): `fitlife-mark.svg` is the compact symbol and `fitlife-logo.svg` is the horizontal README wordmark.
- [`public/favicon.svg`](../../public/favicon.svg) uses the same mark at a browser-friendly small size.
- A future `fitlife-preview.png` should be a 1280 × 640 composition made only from real FitLife captures: Home, Workout Session, and Progress on the existing dark background with the green accent.

The social/repository preview is intentionally not present yet because no real screenshot set existed when the showcase documentation was prepared. Do not add imagined or AI-generated application screens.
